# PostgreSQL Backup configuration file template
# Copy this file to '.env.db' in the same folder and insert your credentials.
# Do not commit '.env.db' to version control systems.

PGPASSWORD=your_secure_postgres_password_here
